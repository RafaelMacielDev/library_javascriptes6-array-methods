// Concatena um ou mais arrays retornando um novo array

const frutas = ['melancia', 'banana', 'uva', 'maça'];
const salgados = ['empada', 'esfirra']
const alimentos = frutas.concat(salgados);
console.log(alimentos)

// Outro exemplo

const arr = [1, 2, 3];
const arr2 = [4, 5, 6];

const novoArr = arr.concat(arr2);

console.log(arr);
console.log(arr2);
console.log(novoArr);