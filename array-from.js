// Cria uma nova instância de array a partir de um parâmetro array-like ou iterable object
// Pode transformar um nodelist em array.

const divArray = Array.from(divs)
divArray.pop()
divArray.shift()

